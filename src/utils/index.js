const products = require('./products')
const firebase = require('./firebase')
const vehicleApi = require('./vehicleApi')
const vehicle = require('./vehicle')

module.exports = {
  products, firebase, vehicleApi, vehicle
}